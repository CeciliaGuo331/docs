{
  "scripts": {
    "generate": "node scripts/generate-content-data.mjs",
    "docs:dev": "npm run generate && vitepress dev",
    "docs:build": "npm run generate && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
    "fs-extra": "^11.3.0",
    "globby": "^14.1.0",
    "gray-matter": "^4.0.3",
    "reading-time": "^1.5.0",
    "vitepress": "1.6.3",
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "markdown-it-mathjax3": "^4.3.2",
    "prettier": "^3.6.1"
  }
}
